<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVVM_Login.Views.LoginPage">
    
        <VerticalStackLayout
            
            HorizontalOptions="CenterAndExpand"
            VerticalOptions="CenterAndExpand">
            <Image
                Source="zeisslogo.jpg"
                WidthRequest="150"
                HeightRequest="150"

                Aspect="AspectFit"
                SemanticProperties.Description="Zeiss Logo" />
            
            <Label
                Text="Welcome to Zeiss Cloud"
                Margin="20"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />
            <Label Text="Username" />
            <Entry Margin="0,0,0,20" Text="{Binding Username}" Placeholder="Enter your username" />
            <Label Text="Password" />
            <Entry Text="{Binding Password}" Placeholder="Enter your password" IsPassword="True"  />
            <Button
                Text="Login"
                Command="{Binding LoginCommand}"
                Margin="20"
                HorizontalOptions="Center"  />
        </VerticalStackLayout>
  
</ContentPage>